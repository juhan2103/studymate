# docker-compose(MySQL)용 환경변수.
# 이 파일을 .env로 복사한 뒤 값을 채운다. (.env 는 gitignore — 커밋 금지)
#
#   cp .env.example .env
#
# 앱 비밀(구글 OAuth, JWT 등)은 여기가 아니라 application-local.yml 에 둔다.

DB_USERNAME=study
DB_PASSWORD=changeme
DB_ROOT_PASSWORD=changeme-root
